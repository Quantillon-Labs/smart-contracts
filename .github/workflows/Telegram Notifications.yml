name: Notify Telegram on Push

on:
  push:
    branches:
      - main
      - dev

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send message to Telegram
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHANNEL_ID: '-1004408312988'
          TELEGRAM_MESSAGE_THREAD_ID: '2'
          # Pass event text through env vars; templating it into the script
          # body lets commit messages inject shell (backticks, quotes).
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          AUTHOR_NAME: ${{ github.event.head_commit.author.name }}
        shell: bash
        run: |
          if [ -z "$TELEGRAM_TOKEN" ] || [ -z "$CHANNEL_ID" ]; then
            echo "Telegram secrets not set; skipping notification."
            exit 0
          fi

          COMMIT_URL="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/commit/$GITHUB_SHA"

          MESSAGE="🚀 New commit on \`$GITHUB_REPOSITORY\` (\`$GITHUB_REF_NAME\` branch)

          📝 Commit: $COMMIT_MESSAGE
          🔗 URL: $COMMIT_URL

          👤 Author: $AUTHOR_NAME"

          curl -fsS --max-time 15 -X POST "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" \
            --data-urlencode "chat_id=$CHANNEL_ID" \
            --data-urlencode "message_thread_id=$TELEGRAM_MESSAGE_THREAD_ID" \
            --data-urlencode "text=$MESSAGE" | jq -e '.ok == true'
