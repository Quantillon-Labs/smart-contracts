[
  {
    "check": "reentrancy-no-eth",
    "file": "src/core/QuantillonVault.sol",
    "function": "_creditVaultYield",
    "reason": "Both callers (creditVaultYield and harvestAndDistributeVaultYield) hold nonReentrant throughout pricing admission and settlement. The linked pricing library calls the configured module; the deployed module has no mutable external callbacks. Price-cache initialization is governance-only. MintExecutionBoundaryAudit explicitly tests adversarial module callbacks into both yield entrypoints, public minting, and initializePriceCache. The flagged internal helper cannot be entered externally."
  },
  {
    "check": "unused-return",
    "file": "src/core/QuantillonVault.sol",
    "function": "_computeMintAmounts",
    "reason": "The function directly returns all three ExecutionPricingLibrary.mint results to its caller. Slither misidentifies the direct tuple return as unused; combined-release integration verifies the resulting mint amount and backing."
  },
  {
    "check": "unused-return",
    "file": "src/oracle/ExecutionPricing.sol",
    "function": "_reference",
    "reason": "The current price, oracle-computed staleness and bounds flags are checked. Historical fallback price and raw update time are deliberately unused; the active oracle computes freshness from its update time."
  },
  {
    "check": "timestamp",
    "file": "src/oracle/ExecutionPricing.sol",
    "function": "publish",
    "reason": "Timestamp comparisons intentionally enforce monotonic observations, reject future or expired depth, and prevent depth reuse before hedge acknowledgment."
  },
  {
    "check": "timestamp",
    "file": "src/oracle/ExecutionPricing.sol",
    "function": "acknowledge",
    "reason": "Timestamp comparisons intentionally reject future, expired, regressing or pre-configuration capacity observations."
  },
  {
    "check": "timestamp",
    "file": "src/oracle/ExecutionPricing.sol",
    "function": "_validate",
    "reason": "Depth and account observations deliberately expire after the configured maximum age; timestamps are used for freshness, not randomness."
  },
  {
    "check": "timestamp",
    "file": "src/core/QuantillonVault.sol",
    "function": "applyDevMode",
    "reason": "The 48-hour admin timelock intentionally compares the current timestamp with its activation deadline."
  },
  {
    "check": "timestamp",
    "file": "src/core/SecureUpgradeable.sol",
    "function": "approveEmergencyDisableSecureUpgrades",
    "reason": "The zero deadline check intentionally verifies that an emergency-disable timelock proposal is active."
  },
  {
    "check": "timestamp",
    "file": "src/core/SecureUpgradeable.sol",
    "function": "applyEmergencyDisableSecureUpgrades",
    "reason": "The emergency-disable path intentionally enforces its 24-hour activation deadline."
  }
]
